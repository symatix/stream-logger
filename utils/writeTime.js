/**
 * 
 * DEPRECATED
 */

// generates a time log to be written at the top of the document

module.exports = function(time){
    return `/*******************/\nTIME: ${time}\n--------------------\n`
}
